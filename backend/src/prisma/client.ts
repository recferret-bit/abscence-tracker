/**
 * Re-exports the Prisma 7 generated client (see prisma/schema.prisma `output`).
 * Application code should import types and enums from here, not from `@prisma/client`.
 */
export * from '../../prisma/generated/client/client';
